"use client";

import SiteHeader from "@/components/SiteHeader";

/** @deprecated Use SiteHeader — kept so existing imports keep working. */
export default function Header() {
  return <SiteHeader />;
}
